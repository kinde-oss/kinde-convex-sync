"use client";

// This is where React components / hooks go.

/**
 * Placeholder stub — NOT a finished export.
 *
 * TODO: Replace with a `useKindeUser`-style hook that wraps Convex's `useQuery`
 * against the synced-user query surface, e.g.
 *
 * ```tsx
 * export function useKindeUser(kindeId: string | undefined) {
 *   return useQuery(
 *     api.myFunctions.getUser,
 *     kindeId ? { kindeId } : "skip",
 *   );
 * }
 * ```
 *
 * It currently returns an empty object because the reactive query surface is
 * not wired up yet; do not rely on it.
 */
export const useMyComponent = () => {
  return {};
};
